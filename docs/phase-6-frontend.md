# Phase 6 Frontend

Phase 6 turns the frontend into a minimal demoable MVP interface.

Implemented UI behavior:

- upload one file
- select platform
- optionally add a caption
- send the form to the backend
- show loading state
- render report details
- show recent saved reports from the backend

The frontend remains intentionally lightweight, but it now exercises the real backend flow instead of serving as a static placeholder.
